/// <reference types="react" />
interface UseCloseOnEscapePressOpts {
    onClose: (e: KeyboardEvent) => void;
    isDisabled?: boolean;
}
/**
 * Calls back when the escape key is pressed.
 * To be used exclusively for closing layered components.
 * Use the `isDisabled` argument to ignore closing events.
 *
 * ```js
 * useCloseOnEscapePress({
 *   onClose: () => {},
 *   isDisabled: false,
 * });
 * ```
 */
export declare function useCloseOnEscapePress({ onClose, isDisabled }: UseCloseOnEscapePressOpts): void;
/**
 *
 * Layering hook to get layering info like the current level, the top level of the given component
 *
 */
export declare function useLayering(): {
    layerList?: import("react").MutableRefObject<string[] | null> | undefined;
    currentLevel: number;
    topLevelRef: import("react").MutableRefObject<number | null>;
    isLayerDisabled: () => boolean;
};
export {};
